import {StyleSheet} from 'react-native';

const sharedStyles = StyleSheet.create({
  bodyContainer: {
    margin: 15,
  },
  buttonSpacing: {
    marginTop: 10,
  },
});

export default sharedStyles;
